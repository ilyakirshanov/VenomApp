services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./instance:/app/instance
    environment:
      - DATABASE_PATH=/app/instance/app.db
      - UPLOAD_FOLDER=uploads